import 'package:flutter/material.dart';

var colorScheme = ColorScheme.fromSeed(seedColor: Colors.greenAccent);
final ThemeData themeData =
    ThemeData(colorScheme: colorScheme, useMaterial3: true);
const textA1 = TextStyle(color: Colors.green, fontWeight: FontWeight.bold);
const textA2 = TextStyle(color: Colors.green, fontWeight: FontWeight.bold);
const aberto =
    TextStyle(color: Colors.blue, fontWeight: FontWeight.bold, fontSize: 10);
const fechado =
    TextStyle(color: Colors.red, fontWeight: FontWeight.bold, fontSize: 10);
